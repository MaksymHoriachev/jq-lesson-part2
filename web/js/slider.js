var mySwiper = new Swiper ('.top-slider-container', {
    // Optional parameters
    direction: 'vertical',
    loop: true,
    autoplay: 4000,
    speed: 1400,
    autoplayDisableOnInteraction: false,

    // If we need pagination
    pagination: '.top-slider-pagination'
});